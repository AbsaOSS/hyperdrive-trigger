# Primary registry - Artifactory npm repository
registry=URL

# Disable public npm registry fallback
# This prevents any accidental usage of registry.npmjs.org
strict-ssl=true

# Prevent telemetry to public npm
audit=false
fund=false
update-notifier=false
